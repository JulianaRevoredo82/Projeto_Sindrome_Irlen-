@model IEnumerable<ProjPortadorSindromeIrlen.Cidade>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cidade1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estado1.Estado1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cidade1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estado1.Estado1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCidade }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCidade }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCidade })
        </td>
    </tr>
}

</table>
